<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100">
  <defs>
    <filter id="glow" x="-20%" y="-20%" width="140%" height="140%">
      <feGaussianBlur stdDeviation="3" result="coloredBlur"/>
      <feMerge>
        <feMergeNode in="coloredBlur"/>
        <feMergeNode in="SourceGraphic"/>
      </feMerge>
    </filter>
  </defs>
  <!-- Hexagon Frame -->
  <path d="M50 5 L90 27 L90 73 L50 95 L10 73 L10 27 Z" fill="none" stroke="#00ff00" stroke-width="4" filter="url(#glow)"/>
  <!-- P3 Text -->
  <text x="50" y="65" font-family="sans-serif" font-weight="900" font-size="38" fill="#00ff00" text-anchor="middle" filter="url(#glow)" style="font-family: 'Orbitron', sans-serif;">P3</text>
  <!-- Decorative small tech lines -->
  <path d="M30 20 L40 20 M60 80 L70 80" stroke="#00ff00" stroke-width="2" opacity="0.5"/>
</svg>
